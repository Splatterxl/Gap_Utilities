const { Client, Message } = require('discord.js');

module.exports = {
    help: null,
    /**
     * 
     * @param {Client} bot 
     * @param {Message} msg 
     * @param {string[]} args 
     */
    run: (bot, msg, args) =>
    {
        // if (msg.channel.id == msg.guild.channels.cache.)
    }
}; 